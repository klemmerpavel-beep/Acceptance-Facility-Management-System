-- Акты выполненных работ: реквизиты исполнителя и отметка подписания.
--
-- Отдельной сущности акта нет намеренно: акт есть представление закрытого
-- транша — «работа траншами: выработали сумму → акт → подписание → оплата»
-- (01_PROJECT.md, раздел 4, шаг 5). Вторая запись тех же строк разошлась бы
-- с первой при первом же сторно приёмки.
--
-- Обе колонки необязательны, значений по умолчанию не требуют: миграция
-- безоговорочна.

-- AlterTable
ALTER TABLE "organizations" ADD COLUMN "requisites" TEXT;

-- AlterTable
ALTER TABLE "tranches" ADD COLUMN "signedAt" DATE;
