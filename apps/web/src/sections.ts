/**
 * Разделы верхнего уровня.
 *
 * Состав и порядок взяты у эталона, но не слепо: раздел людей назван
 * «Контактами» решением заказчика от 12.09.2026 — у эталона он «Контрагенты».
 * Состав: Главная, Заявки, Проекты, Контакты,
 * Бухгалтерия — и «Ещё» со списком служебных экранов, который живёт в самой
 * шапке, а не здесь.
 *
 * Это отменяет прежнее решение о трёх разделах и правило карты разделов
 * «раздел появляется тогда и только тогда, когда за ним стоит экран с
 * данными». Отмена — решение заказчика от 05.09.2026, записанное в
 * `docs/07_IA.md`. Прежнее правило существовало затем, чтобы в шапке не
 * появлялись пункты без содержания; взамен него действует другое: пункт
 * без своего экрана ведёт на «Что дальше», где названа его стадия, а не в
 * пустую заглушку.
 */
export const SECTIONS = [
  { key: "home", label: "Главная", icon: "#i-home" },
  { key: "requests", label: "Заявки", icon: "#i-request" },
  { key: "projects", label: "Проекты", icon: "#i-object" },
  { key: "contacts", label: "Контакты", icon: "#i-people" },
  { key: "accounting", label: "Бухгалтерия", icon: "#i-money" },
] as const;

/**
 * Разделы, за которыми ещё нет экрана: ведут на «Что дальше».
 *
 * Список пуст: у каждого раздела навигации есть свой экран. Пустой массив
 * оставлен намеренно — правило «раздел без экрана ведёт на „Что дальше“»
 * остаётся в силе и сработает, когда в шапку добавят следующий раздел.
 */
export const PLANNED_SECTIONS: readonly Section[] = [];

export type Section = (typeof SECTIONS)[number]["key"] | "settings" | "roadmap";

/**
 * Что появится дальше. Один список на весь продукт: и разделы шапки, и
 * вкладки карточки объекта. У каждой строки — что раздел будет делать и
 * на какой стадии появится. Обещание собрано в одном месте, а не рассыпано
 * по навигации заглушками.
 */
export const ROADMAP: readonly {
  title: string;
  where: "раздел" | "вкладка объекта";
  stage: string;
  text: string;
}[] = [
  {
    title: "Почтовый шлюз чеков",
    where: "вкладка объекта",
    stage: "стадия D",
    text: "Чеки заводятся формой, и вкладка работает. Не сделан приём писем: адрес вида checks+КОД@dolgiy.studio превращал бы письмо в черновик расхода сам. Ждёт подтверждения домена компании и подадресации на почтовом сервере.",
  },
  {
    title: "Документы организации",
    where: "раздел",
    stage: "отложенный контур",
    text: "Шаблоны договоров и актов с переменными, конструктор документа.",
  },
];
